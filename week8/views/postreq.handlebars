<h1>You sent the following data in a {{this.type}} request:</h1>
<h5> in order of importance (EG query then body requests) </h5>
<ul>
  {{#each dataList}}
    <li>{{this.name}}: {{this.value}}
  {{/each}}
</ul>
